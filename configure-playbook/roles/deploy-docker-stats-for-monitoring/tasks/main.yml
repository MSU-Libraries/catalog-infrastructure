---

- name: Deploy pc-docker-stats script
  ansible.builtin.copy:
    src: pc-docker-stats
    dest: /usr/local/bin/
    mode: '0755'
    owner: root
    group: root

- name: Deploy docker_stats cron file
  ansible.builtin.copy:
    src: docker_stats
    dest: /etc/cron.d/
    mode: '0644'
    owner: root
    group: root
